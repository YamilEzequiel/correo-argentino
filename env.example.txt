# Renombrá este archivo a `.env` y completá los valores.
# IMPORTANTE: `.env` está (o debería estar) en .gitignore. NO subas credenciales reales al repo.

# ─── Credenciales obligatorias ─────────────────────────────────
USER_TOKEN=
PASSWORD_TOKEN=

# ─── Ambiente: TEST | PROD ─────────────────────────────────────
ENVIRONMENT=TEST

# ─── Modo de inicialización (elegí UNO) ────────────────────────
# Modo A: si tenés CUSTOMER_ID, completalo y dejá email/password vacíos.
# La lib usará initializeWithCustomerId (omite /users/validate).
CUSTOMER_ID=

# Modo B: si NO tenés CUSTOMER_ID, dejá vacío CUSTOMER_ID y completá email/password.
# La lib usará initializeAll (obtiene customerId vía /users/validate).
EMAIL=
PASSWORD=

# ─── Server local del playground (opcional) ────────────────────
PORT=3000
